apiVersion: v1
kind: Service
metadata:
  name: argocd-helloworldd
  annotations:
    notifications.v1/subscribe.on-sync-succeeded.slack: argocd
spec:
  selector:
    app: argocd-helloworld
  ports:
    - port: 80
      targetPort: 80
